<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/App.ThemeNoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".Activities.activities.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.activities.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.activities.QuestionActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.Quizz" />
        <activity
            android:name=".Activities.activities.LogInIntro"
            android:exported="true" />
        <activity
            android:name=".Activities.activities.SignUp"
            android:exported="false" />
        <activity
            android:name=".Activities.activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>